import Router from '../router/Router';
import { StyContent } from '../styled/styled';

const Content = () => {
    return (
        <StyContent className="content">
            <Router />
        </StyContent>
    );
};

export default Content;
